#include <stdio.h>

/**
 * main - A program that print numbers 0 to 9
 *
 * Return: 0 (Success)
 */

int main(void)
{
	int m;

	for (m = 0; m <= 9; m++)
	{
		putchar(m);
	}
	putchar('\n');

	return (0);
}
